using UnityEngine;

public class JesseTheGreatSTinky : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    float five = 5; 
    void Start()
    {
     float jesse=6;   
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(five);
    }
}
